import { LoginContainer, LoginTitle, StyledLogo } from "./Login.style";

const Login = () => {
  return (
    <LoginContainer>
      <StyledLogo />
      <LoginTitle>Login to your account</LoginTitle>
    </LoginContainer>
  );
};

export default Login;
